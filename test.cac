//include "hello.cac"; // zaprintitln
//
//zaprintitln("Hello world!");
//
//var a = FILE("tests/test.txt");
//var text = a.read(0, 0);
//zaprintitln(text);
//a.write(len(text), "\nHello from test.cac!");

func push(list, value) {
    list[len(list)] = value;
}

var a = [23, "string", 2];

zaprintit str(a) + "\n";

zaprintit "finished\n";
zaprintit str(a) + "\n";
